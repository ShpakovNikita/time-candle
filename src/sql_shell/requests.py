def get_last_id():
    """
    Get last id of the user, added in database. This will need for new users id
    generation
    :return:
    """
    return 1


def find_user(login):
    """
    Find name of the user in database. If not, return None
    :return:
    """
    if True:
        return {'uid': 1, 'preferences': {'password': '1111'}}
    else:
        return None
